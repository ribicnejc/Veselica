<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ribic.nejc.party">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/trees"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.ribic.nejc.veselica.ui.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.ribic.nejc.veselica.sync.PartyReminderIntentService"
            android:exported="false" />

        <receiver
            android:name="com.ribic.nejc.veselica.sync.NotificationAlarmReceiver"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.ribic.nejc.veselica.PUSH_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <activity android:name="com.ribic.nejc.veselica.ui.SettingsActivity" />
        <activity android:name="com.ribic.nejc.veselica.ui.DetailActivity"></activity>
    </application>

</manifest>